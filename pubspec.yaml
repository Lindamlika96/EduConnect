name: educonnect_mobile
description: "Mobile EduConnect App"
publish_to: 'none' # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Logging package
  logger: ^2.0.2

  # Session management
  shared_preferences: ^2.3.2

  # Icons for iOS style UI
  cupertino_icons: ^1.0.8

  # 🧩 Base de données locale
  sqflite: ^2.3.3
  path: ^1.8.3

  # ✨ Animation package
  flutter_animate: ^4.5.0

  http: ^1.2.0  # ✅ ajoute cette ligne


dev_dependencies:
  flutter_test:
    sdk: flutter

  # SQLite support for testing on desktop/VM
  sqflite_common_ffi: ^2.3.3+1

  # Recommended lint rules
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Exemple pour les futurs assets
  # assets:
  #   - assets/images/
  #
  # Exemple pour les polices
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
