name: educonnect_mobile
description: "Mobile EduConnect App"
publish_to: 'none' # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Icons for iOS style UI
  cupertino_icons: ^1.0.8

  # 🧩 Base de données locale
  sqflite: ^2.3.3
  path: ^1.8.3
  pdfx: ^2.6.0  
  confetti: ^0.7.0  
  fl_chart: ^0.68.0 
  pdf: ^3.10.7  
  path_provider: ^2.1.2
  open_filex: ^4.3.2 
  http: ^1.1.0
  printing: ^5.12.0
  lottie: ^2.2.0 # 👈 ajoute cette ligne ici
  screen_protector: ^1.4.5
  google_fonts: ^6.0.0
  


dev_dependencies:
  flutter_test:
    sdk: flutter

  # Recommended lint rules
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/pdfs/
    - assets/animations/learing.json
    - assets/animations/level_up_easy.json
    - assets/animations/level_up_hard.json
    - assets/animations/level_up_medium.json
    - assets/animations/welcome.json
    - assets/animations/learning.json
    - assets/animations/book.json
    -  assets/animations/ai_bot.json
    - assets/animations/celebrate.json
    - assets/animations/podium.json



  # Exemple pour les futurs assets
  # assets:
  #   - assets/images/
  #
  # Exemple pour les polices
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
